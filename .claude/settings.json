{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *lib/pricing/*|*lib/services/*) cd \"${CLAUDE_PROJECT_DIR:-.}\" && out=$(pnpm vitest run --silent 2>&1) || { { echo 'Para-kritik test HATASI (Kural 6) — düzeltmeden devam etme:'; echo \"$out\" | tail -25; } >&2; exit 2; } ;; esac",
            "timeout": 180,
            "statusMessage": "Para-kritik testler koşuyor (Kural 6)…"
          }
        ]
      }
    ]
  }
}
